@model IEnumerable<Booking.Models.tipoQuarto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("criar novo", "Create")
</p>

<div id="pesquisa">
    @using (Html.BeginForm("Index", "Tipo de Quarto", FormMethod.Post))
    {
        <input type="text" placeholder="Insira o tipo de quarto a procurar" name="texto" />
        <input type="submit" name="Procurar" value="Procurar" />

    }


</div>
<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Inventario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Capacidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayNameFor(model => model.Inventario)
    </td>
    <td>
        @Html.DisplayNameFor(model => model.Capacidade)
    </td>
    <td>
        @Html.DisplayNameFor(model => model.Descricao)
    </td>

    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.IDTipoQaurto }) |
        @Html.ActionLink("Details", "Details", new { id = item.IDTipoQaurto }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.IDTipoQaurto })
    </td>
</tr>
    }

</table>
